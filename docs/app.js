import Vue from 'vue';
import { TableColumn, TableComponent, TableColumnFilter } from '../src';

new Vue({
    el: '#app',

    components: {
        TableColumn,
        TableComponent,
        TableColumnFilter
    },
});
